# codebase-sync-dump-restore
Python utility that recursively flattens codebase project directories into a single, semantic Org-mode context map. Allows for LLM context-injection by providing the full project structure and content in one ingestion-friendly document.
